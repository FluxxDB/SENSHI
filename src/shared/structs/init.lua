local structs = {
  item = require(script.drop),
  entity = require(script.entity),
  resource = require(script.resource),
  transform = require(script.transform),
  status = require(script.status),
}

return structs